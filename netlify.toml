[build]
  # Command to build your Angular app
  command = "ng build --configuration production"
  # Folder where Angular puts the built files
  publish = "dist/ElectroHub11/browserS"

# Redirect all routes to index.html for Angular routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
